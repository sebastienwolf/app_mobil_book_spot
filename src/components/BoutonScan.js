import { StyleSheet, Button as ButtonScanner } from "react-native";

export default function Button({text}) {

  return (
    <ButtonScanner color="#841584">
    {text}
  </ButtonScanner>

  );
}



const styles = StyleSheet.create({
 
});